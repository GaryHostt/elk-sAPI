# HTTP Configuration
http.host=0.0.0.0
http.port=8081

# MCP Server Configuration (for agent tool integration)
mcp.serverName=elk-mcp-server
mcp.serverVersion=1.0.0

# Elasticsearch Configuration
elasticsearch.host=localhost
elasticsearch.port=9200
elasticsearch.url=http://${elasticsearch.host}:${elasticsearch.port}

# Default Index Configuration
elasticsearch.default.index=my-index

# Logging Configuration
logging.level=INFO
